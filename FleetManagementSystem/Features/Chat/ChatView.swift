import SwiftUI

struct ChatView: View {
    var body: some View {
        NavigationView {
            Text("Chat")
                .navigationTitle("Chat")
        }
    }
}

#Preview {
    ChatView()
}
